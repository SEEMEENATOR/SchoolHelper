<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Секция</title>
    <style>
        /* Подключаем шрифт Montserrat */
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&display=swap');

        /* Общие стили */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Montserrat', sans-serif;
        }

        /* Стиль для body */
        body {
            background-color: #f4f4f9;
            color: #333;
            font-size: 16px;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column; /* Вертикальное расположение */
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            background-image: url('../src/blur_fon.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }

        /* Заголовок секции */
        h1 {
            color: #380470;
            font-weight: 600;
            text-align: center;
            margin-bottom: 20px;
            font-size: 32px;
        }

        /* Описание секции */
        p {
            font-size: 18px;
            text-align: left;
            color: #555;
            line-height: 1.5;
            margin-bottom: 20px;
            background-color: #fff;
            padding: 15px 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 800px;
            width: 100%;
        }

        /* Адаптивные стили */
        @media (max-width: 768px) {
            h1 {
                font-size: 24px;
            }

            p {
                font-size: 16px;
                padding: 10px 15px;
            }
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 20px;
            }

            p {
                font-size: 14px;
                padding: 8px 12px;
            }
        }
        .back-link {
            position: absolute;
            top: 10px;
            right: 10px;
            text-decoration: none;
            padding: 10px 15px;
            background-color: #8b00ff;
            color: white;
            border-radius: 5px;
            font-weight: 500;
            font-size:24px;
            transition: background-color 0.3s;
        }

        .back-link:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<a href="/" class="back-link">Назад</a>
{{#section}}
    <h1>{{#title}}{{title}}{{/title}}{{^title}}Заголовок раздела отсутствует{{/title}}</h1>
    <p>{{#description}}{{description}}{{/description}}{{^description}}Описание отсутствует{{/description}}</p>
{{/section}}
</body>
</html>
